maintainer : streambinder
name       : paperboy
version    : 5
release    : 6
source     :
    - https://github.com/streambinder/paperboy/archive/v5.tar.gz : e94bd2df15d98c5f45a67ac368acce6c3bf27f4a20309af1b258f7b3f36f8dea
license    : GPL-3.0
component  : network.mail
summary    : IMAP email checker and GTK notifier
description: |
    Paperboy is a simple CLI application which sends a GTK notification when an email is received, allowing the user to just click on the callback button to open the webmail associated to the account.
builddeps  :
    - pkgconfig(gio-2.0)
    - pkgconfig(libcurl)
    - pkgconfig(libnotify)
    - pkgconfig(yaml-0.1)
build      : |
    %make
install    : |
    mkdir -p ${installdir}/usr/sbin
    %make_install
