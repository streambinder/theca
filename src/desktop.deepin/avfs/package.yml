maintainer : Justin
name       : avfs
version    : 1.1.2
release    : 2
source     :
    - https://sourceforge.net/projects/avf/files/avfs/1.1.2/avfs-1.1.2.tar.bz2/download : b69339d3602221bc381fd7528e6dda4b23522f825781638be3fa7d9b8837ab0c
license    :
    - GPL-2.0-or-later
    - LGPL-2.0-or-later
component  : system.utils
summary    : AVFS enables all programs to access archived, compressed, or remote files transparently.
description: |
    AVFS is a system which enables all programs to look inside archived or compressed files, or access remote files without recompiling the programs or changing the kernel.
rundeps    :
    - perl
builddeps  :
    - pkgconfig(fuse)
    - pkgconfig(liblzma)
setup      : |
    %configure
build      : |
    %make
install    : |
    %make_install
