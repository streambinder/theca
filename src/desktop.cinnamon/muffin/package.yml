name       : muffin
version    : 4.4.3
release    : 3
source     :
    - https://github.com/linuxmint/muffin/archive/4.4.3.tar.gz : 2edf06544be8bdead33902424b796786f0d3cd0ae3b73c3c78f2321452bf5784
license    : GPL-2.0
component  : desktop.cinnamon
summary    : Cinnamon window manager based on Mutter
description: |
    The window management library for the Cinnamon desktop (libmuffin) and its sample WM binary (muffin).
builddeps  :
    - pkgconfig(cinnamon-desktop)
    - pkgconfig(clutter-1.0)
    - pkgconfig(cogl-1.0)
    - pkgconfig(glib-2.0)
    - pkgconfig(gtk+-3.0)
    - pkgconfig(libcanberra)
    - pkgconfig(libstartup-notification-1.0)
    - pkgconfig(sm)
    - pkgconfig(xcursor)
    - pkgconfig(xinerama)
    - pkgconfig(xkeyboard-config)
    - gnome-common
    - gnome-doc-utils
    - gtk-doc
    - zenity
setup      : |
    sed -i 's/--warn-error//g' src/Makefile.am
    %autogen --prefix=/usr --disable-static --libdir=/usr/lib64 --libexecdir=/usr/lib64 --disable-gtk-doc-html --disable-wayland-egl-server
build      : |
    %make
install    : |
    %make_install
