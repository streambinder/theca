name       : cinnamon
version    : 4.4.8
release    : 4
source     :
    - https://github.com/linuxmint/cinnamon/archive/4.4.8.tar.gz : 5bc7db4e519aed8b3b76d6b68abc02be966633005a92579071a77d23d33c8af4
license    : GPL-2.0
component  : desktop.cinnamon
summary    : A Linux desktop featuring a traditional layout, built from modern technology and introducing brand new innovative features
description: |
    Cinnamon is a Linux desktop which provides advanced innovative features and a traditional user experience.
    The desktop layout is similar to Gnome 2 with underlying technology forked from Gnome Shell.
    Cinnamon makes users feel at home with an easy to use and comfortable desktop experience.
builddeps  :
    - pkgconfig(cinnamon-desktop)
    - pkgconfig(cjs-1.0)
    - pkgconfig(clutter-1.0)
    - pkgconfig(dbus-glib-1)
    - pkgconfig(gconf-2.0)
    - pkgconfig(gdk-pixbuf-2.0)
    - pkgconfig(gio-2.0)
    - pkgconfig(gnome-keyring-1)
    - pkgconfig(gtk+-3.0)
    - pkgconfig(libcinnamon-menu-3.0)
    - pkgconfig(libcroco-0.6)
    - pkgconfig(libmuffin)
    - pkgconfig(libnm)
    - pkgconfig(libpulse)
    - pkgconfig(libsoup-2.4)
    - pkgconfig(libstartup-notification-1.0)
    - pkgconfig(xfixes)
    - glib-networking
    - gtk-doc
rundeps    :
    - caribou
    - libtimezonemap
    - python-pexpect
    - python-pam
    - python-pillow
    - python-tinycss
    - python-pytz
setup      : |
    %autogen --prefix=/usr \
        --sysconfdir=/etc \
        --libexecdir=/usr/lib64/cinnamon \
        --localstatedir=/var \
        --disable-static \
        --enable-compile-warnings=yes \
        --with-session-tracking=systemd \
        --disable-gtk-doc
build      : |
    %make
install    : |
    %make_install
